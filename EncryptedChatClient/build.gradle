
jar {
    manifest {
        attributes 'Main-Class': 'top.starcatmeow.chat.client.Main'
    }
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
dependencies{
    compile files('../libs/beautyeye_lnf.jar')
}